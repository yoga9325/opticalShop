export interface Lens {
    id: number;
    name: string;
    description: string;
    price: number;
    imageUrl?: string;
}

export interface LensCoating {
    id: number;
    name: string;
    description: string;
    price: number;
    imageUrl?: string;
}
